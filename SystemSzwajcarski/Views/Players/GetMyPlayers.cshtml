@model IEnumerable<SystemSzwajcarski.Models.RelationOP>
<h1>Lista zawodników</h1>
<ul>
    @foreach (var player in Model)
    {
        <li>

            @player.Player.Name - @player.Player.LastName - @player.Ranking

        </li>
    }
</ul>
